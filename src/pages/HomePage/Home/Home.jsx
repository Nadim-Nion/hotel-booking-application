import React from 'react';
import SearchBar from '../../../components/SearchBar/SearchBar';

const Home = () => {
    return (
        <div>
            <SearchBar></SearchBar>
        </div>
    );
};

export default Home;